'use client'

import { useEffect, useMemo, useState } from 'react'
import { usePathname, useRouter } from 'next/navigation'
import { BarChart3, Bell, BookOpen, Check, ChevronLeft, ChevronRight, CircleHelp, Clock3, FileQuestion, Home, LayoutDashboard, Menu, MoreHorizontal, Plus, Search, Settings, ShieldAlert, Sparkles, Trophy, Users, X, Zap } from 'lucide-react'
import { Button } from '@/components/ui/button'

type Question = { id: number; text: string; options: string[]; correct: number; marks: number }
type Quiz = { id: string; title: string; subject: string; description: string; duration: number; questions: Question[]; status: 'Published' | 'Draft' | 'Completed'; attempts: number; totalMarks: number }
type Attempt = { id: string; quizId: string; quizName: string; date: string; score: number; percentage: number; status: string; time: string; tabSwitches: number }

const seedQuestions = (title: string): Question[] => [
  { id: 1, text: title === 'Computer Networks' ? 'Which protocol is used to translate domain names into IP addresses?' : `Which statement best describes a core concept in ${title}?`, options: ['DNS', 'FTP', 'SSH', 'SMTP'], correct: 0, marks: 10 },
  { id: 2, text: 'Which layer is responsible for reliable end-to-end delivery?', options: ['Physical', 'Transport', 'Network', 'Application'], correct: 1, marks: 10 },
  { id: 3, text: 'What is the primary benefit of using a structured approach?', options: ['Faster hardware', 'Clearer solutions', 'More storage', 'Less testing'], correct: 1, marks: 10 },
  { id: 4, text: 'Which option represents a best practice?', options: ['Skip planning', 'Test and iterate', 'Avoid feedback', 'Ignore edge cases'], correct: 1, marks: 10 },
]
const initialQuizzes: Quiz[] = ['Computer Networks', 'Data Structures', 'Database Management Systems', 'Artificial Intelligence', 'Operating Systems'].map((title, i) => ({ id: `q-${i + 1}`, title, subject: ['Networking', 'Computer Science', 'Databases', 'Machine Learning', 'Systems'][i], description: 'Build confidence with this practice assessment.', duration: [30, 45, 25, 40, 35][i], questions: seedQuestions(title), status: i < 4 ? 'Published' : 'Draft', attempts: [128, 96, 74, 61, 0][i], totalMarks: 40 }))
const leaderboard = [
  ['Alex Morgan', 95, '95%', '12:20'], ['Jordan Lee', 91, '91%', '13:10'], ['Priya Shah', 88, '88%', '14:02'], ['You', 82, '82%', '15:14'], ['Sam Wilson', 79, '79%', '16:40'],
]

function useLocalState<T>(key: string, initial: T) {
  const [value, setValue] = useState<T>(initial)
  useEffect(() => { try { const saved = localStorage.getItem(key); if (saved) setValue(JSON.parse(saved)) } catch {} }, [key])
  useEffect(() => { try { localStorage.setItem(key, JSON.stringify(value)) } catch {} }, [key, value])
  return [value, setValue] as const
}

function Logo() { return <div className="flex items-center gap-2 font-semibold tracking-tight"><div className="grid size-9 place-items-center rounded-xl bg-blue-600 text-white shadow-lg shadow-blue-200"><Sparkles className="size-5" /></div><span className="text-xl">Quiz<span className="text-blue-600">Pro</span></span></div> }
function PageButton({ children, onClick, variant = 'outline' }: { children: React.ReactNode; onClick?: () => void; variant?: 'outline' | 'default' | 'ghost' }) { return <Button variant={variant} onClick={onClick} className="rounded-xl">{children}</Button> }

function Landing() { const router = useRouter(); return <main className="min-h-screen bg-[#f7f9fc] text-slate-900">
  <header className="mx-auto flex max-w-7xl items-center justify-between px-5 py-5 lg:px-8"><Logo /><nav className="hidden items-center gap-8 text-sm font-medium text-slate-500 md:flex"><a href="#home">Home</a><a href="#about">About</a><button onClick={() => router.push('/teacher')}>Teacher</button><button onClick={() => router.push('/student')}>Student</button></nav><PageButton onClick={() => router.push('/student')} variant="default">Login <ChevronRight data-icon="inline-end" /></PageButton></header>
  <section id="home" className="mx-auto grid max-w-7xl items-center gap-16 px-5 pb-20 pt-14 lg:grid-cols-[1.05fr_.95fr] lg:px-8 lg:pb-32 lg:pt-24"><div><div className="mb-6 inline-flex items-center gap-2 rounded-full border border-blue-100 bg-blue-50 px-3 py-1.5 text-xs font-semibold text-blue-700"><Zap className="size-3.5" /> Learn smarter, together</div><h1 className="max-w-xl text-5xl font-bold leading-[1.06] tracking-[-0.05em] text-slate-950 sm:text-6xl">Interactive <span className="text-blue-600">Quiz</span> Platform</h1><p className="mt-6 max-w-lg text-lg leading-8 text-slate-500">Create. Attempt. Compete. An interactive platform where teachers can create quizzes and students can attempt them in a timed, monitored environment.</p><div className="mt-9 flex flex-wrap gap-3"><PageButton variant="default" onClick={() => router.push('/teacher')}>Teacher Portal <ChevronRight data-icon="inline-end" /></PageButton><PageButton onClick={() => router.push('/student')}>Student Portal</PageButton></div><div className="mt-12 flex items-center gap-8 text-sm"><div><p className="text-2xl font-bold text-slate-900">12k+</p><p className="text-slate-500">Active learners</p></div><div><p className="text-2xl font-bold text-slate-900">4.9/5</p><p className="text-slate-500">Platform rating</p></div></div></div><div className="relative mx-auto w-full max-w-[530px]"><div className="absolute -inset-6 rounded-[3rem] bg-gradient-to-br from-blue-100 via-indigo-50 to-purple-100 blur-2xl"/><div className="relative overflow-hidden rounded-[2rem] border border-white bg-white p-5 shadow-2xl shadow-blue-100"><div className="flex items-center justify-between border-b border-slate-100 pb-4"><div className="flex items-center gap-3"><div className="grid size-10 place-items-center rounded-xl bg-blue-50 text-blue-600"><FileQuestion className="size-5" /></div><div><p className="text-sm font-semibold">Computer Networks</p><p className="text-xs text-slate-400">Question 4 of 10</p></div></div><div className="flex items-center gap-1.5 rounded-lg bg-orange-50 px-2.5 py-1.5 text-xs font-bold text-orange-600"><Clock3 className="size-3.5" /> 14:32</div></div><div className="py-8"><p className="mb-6 text-xl font-semibold leading-8 text-slate-800">Which protocol is used to translate domain names into IP addresses?</p><div className="grid gap-3">{['DNS', 'FTP', 'SSH', 'SMTP'].map((x, i) => <div key={x} className={`flex items-center gap-3 rounded-xl border p-4 text-sm ${i === 0 ? 'border-blue-500 bg-blue-50 text-blue-700' : 'border-slate-100 text-slate-600'}`}><div className={`grid size-6 place-items-center rounded-full border text-xs font-bold ${i === 0 ? 'border-blue-500 bg-blue-600 text-white' : 'border-slate-200'}`}>{i === 0 ? <Check className="size-3.5" /> : String.fromCharCode(65 + i)}</div>{x}{i === 0 && <span className="ml-auto rounded-full bg-blue-600 px-2 py-0.5 text-[10px] font-bold text-white">Selected</span>}</div>)}</div></div><div className="flex items-center justify-between border-t border-slate-100 pt-4 text-xs text-slate-400"><span>Tab switches: 0 / 3</span><div className="flex gap-2"><span className="rounded-lg bg-slate-100 px-3 py-2">Previous</span><span className="rounded-lg bg-blue-600 px-3 py-2 font-semibold text-white">Next question</span></div></div></div></div></section>
  <section id="about" className="border-t border-slate-100 bg-white px-5 py-16"><div className="mx-auto max-w-7xl"><p className="text-sm font-semibold uppercase tracking-widest text-blue-600">Everything in one place</p><h2 className="mt-3 text-3xl font-bold tracking-tight">A better way to assess progress.</h2><div className="mt-8 grid gap-4 md:grid-cols-3">{[['Create with ease','Build beautiful quizzes with a focused question builder.',FileQuestion],['Stay in the flow','Timed attempts and clear feedback keep students engaged.',Clock3],['See the bigger picture','Track performance with insights that make sense.',BarChart3]].map(([t,d,I]) => <div className="rounded-2xl border border-slate-100 bg-slate-50 p-6" key={String(t)}><div className="mb-5 grid size-11 place-items-center rounded-xl bg-white text-blue-600 shadow-sm"><I className="size-5" /></div><h3 className="font-semibold">{String(t)}</h3><p className="mt-2 text-sm leading-6 text-slate-500">{String(d)}</p></div>)}</div></div></section>
</main> }

const teacherNav = [['Dashboard','/teacher',LayoutDashboard],['Create Quiz','/teacher/create-quiz',Plus],['My Quizzes','/teacher/quizzes',FileQuestion],['Responses','/teacher/results',BarChart3],['Leaderboard','/teacher/leaderboard',Trophy],['Profile','#',Users]] as const
const studentNav = [['Dashboard','/student',LayoutDashboard],['Available Quizzes','/student/quizzes',BookOpen],['My Attempts','/student/attempts',FileQuestion],['Leaderboard','/student/leaderboard',Trophy],['Profile','#',Users]] as const
function AppShell({ role, children }: { role: 'teacher' | 'student'; children: React.ReactNode }) { const pathname = usePathname(); const router = useRouter(); const [open, setOpen] = useState(false); const nav = role === 'teacher' ? teacherNav : studentNav; return <div className="min-h-screen bg-[#f7f9fc] text-slate-900"><aside className={`fixed inset-y-0 left-0 z-30 flex w-64 flex-col border-r border-slate-100 bg-white px-4 py-5 transition-transform lg:translate-x-0 ${open ? 'translate-x-0' : '-translate-x-full'}`}><div className="px-3"><Logo /></div><div className="mt-10 flex flex-1 flex-col gap-1">{nav.map(([label, href, Icon]) => <button key={label} onClick={() => { if (href !== '#') router.push(href); setOpen(false) }} className={`flex items-center gap-3 rounded-xl px-3 py-3 text-left text-sm font-medium transition ${pathname === href ? 'bg-blue-50 text-blue-700' : 'text-slate-500 hover:bg-slate-50 hover:text-slate-900'}`}><Icon className="size-[18px]" />{label}</button>)}</div><div className="rounded-2xl bg-slate-50 p-4"><div className="flex items-center gap-2 text-xs font-semibold text-slate-700"><ShieldAlert className="size-4 text-blue-600" /> Demo mode</div><p className="mt-2 text-xs leading-5 text-slate-400">Your data is saved locally in this browser.</p></div></aside>{open && <button aria-label="Close menu" className="fixed inset-0 z-20 bg-slate-900/20 lg:hidden" onClick={() => setOpen(false)} /> }<div className="lg:pl-64"><header className="sticky top-0 z-10 flex h-[76px] items-center justify-between border-b border-slate-100 bg-white/90 px-5 backdrop-blur lg:px-9"><div className="flex items-center gap-3"><button aria-label="Open menu" className="rounded-lg p-2 hover:bg-slate-100 lg:hidden" onClick={() => setOpen(true)}><Menu className="size-5" /></button><div><p className="text-xs font-medium text-slate-400">{role === 'teacher' ? 'Teacher workspace' : 'Student workspace'}</p><h1 className="text-lg font-semibold">{role === 'teacher' ? 'Good morning, Maria' : 'Good morning, Alex'}</h1></div></div><div className="flex items-center gap-3"><button aria-label="Notifications" className="relative rounded-xl p-2.5 text-slate-500 hover:bg-slate-50"><Bell className="size-5" /><span className="absolute right-2 top-2 size-1.5 rounded-full bg-blue-600" /></button><div className="grid size-9 place-items-center rounded-full bg-gradient-to-br from-blue-500 to-indigo-600 text-sm font-bold text-white">{role === 'teacher' ? 'MR' : 'AM'}</div></div></header><main className="mx-auto max-w-[1400px] p-5 lg:p-9">{children}</main></div></div> }
function SectionHeader({ eyebrow, title, description, action }: { eyebrow?: string; title: string; description?: string; action?: React.ReactNode }) { return <div className="mb-8 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div>{eyebrow && <p className="mb-2 text-xs font-bold uppercase tracking-widest text-blue-600">{eyebrow}</p>}<h2 className="text-3xl font-bold tracking-tight text-slate-950">{title}</h2>{description && <p className="mt-2 text-sm text-slate-500">{description}</p>}</div>{action}</div> }
function StatCard({ label, value, detail, icon: Icon, tone }: { label:string; value:string; detail:string; icon:any; tone:string }) { return <div className="rounded-2xl border border-slate-100 bg-white p-5 shadow-sm"><div className="flex items-start justify-between"><div><p className="text-sm text-slate-500">{label}</p><p className="mt-2 text-3xl font-bold tracking-tight">{value}</p></div><div className={`grid size-10 place-items-center rounded-xl ${tone}`}><Icon className="size-5" /></div></div><p className="mt-4 text-xs font-medium text-slate-400">{detail}</p></div> }
function Dashboard({ role, quizzes, attempts }: { role:'teacher'|'student'; quizzes:Quiz[]; attempts:Attempt[] }) { const router=useRouter(); const student=role==='student'; return <><SectionHeader eyebrow="Overview" title={student ? 'Your learning hub' : 'Your teaching hub'} description={student ? 'Keep your momentum going with today’s practice.' : 'Here’s what’s happening across your quizzes.'} action={<PageButton variant="default" onClick={() => router.push(student?'/student/quizzes':'/teacher/create-quiz')}><Plus data-icon="inline-start" />{student?'Browse quizzes':'Create quiz'}</PageButton>} /><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{(student ? [['Available quizzes',String(quizzes.filter(q=>q.status==='Published').length),'Ready when you are',BookOpen,'bg-blue-50 text-blue-600'],['Completed quizzes',String(attempts.length),'Keep it up',Check,'bg-emerald-50 text-emerald-600'],['Average score','82%','+8% this month',BarChart3,'bg-violet-50 text-violet-600'],['Leaderboard rank','#4','Top 10% of learners',Trophy,'bg-orange-50 text-orange-600']] : [['Total quizzes',String(quizzes.length),'+2 this month',FileQuestion,'bg-blue-50 text-blue-600'],['Active quizzes',String(quizzes.filter(q=>q.status==='Published').length),'Across 4 subjects',Zap,'bg-emerald-50 text-emerald-600'],['Total students','248','+12% this month',Users,'bg-violet-50 text-violet-600'],['Total attempts',String(quizzes.reduce((a,q)=>a+q.attempts,0)),'+18% this month',BarChart3,'bg-orange-50 text-orange-600']]).map(([label,value,detail,Icon,tone])=><StatCard key={String(label)} label={String(label)} value={String(value)} detail={String(detail)} icon={Icon} tone={String(tone)} />)}</div><div className="mt-8 grid gap-6 xl:grid-cols-[1.35fr_.65fr]"><div className="rounded-2xl border border-slate-100 bg-white p-6 shadow-sm"><div className="flex items-center justify-between"><div><h3 className="font-semibold">{student?'Continue learning':'Recent activity'}</h3><p className="mt-1 text-sm text-slate-400">{student?'Pick up where you left off.':'Performance across your latest assessments.'}</p></div><button className="text-sm font-semibold text-blue-600" onClick={()=>router.push(student?'/student/quizzes':'/teacher/quizzes')}>View all</button></div><div className="mt-6 flex flex-col gap-3">{quizzes.slice(0,4).map((q,i)=><div key={q.id} className="flex items-center gap-4 rounded-xl border border-slate-100 p-3"><div className="grid size-10 place-items-center rounded-xl bg-blue-50 text-blue-600"><BookOpen className="size-4" /></div><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold">{q.title}</p><p className="mt-1 text-xs text-slate-400">{q.subject} · {q.questions.length} questions · {q.duration} min</p></div>{student?<PageButton onClick={()=>router.push(`/student/quiz/${q.id}`)}>Start</PageButton>:<span className="text-xs font-medium text-slate-400">{q.attempts} attempts</span>}</div>)}</div></div><div className="rounded-2xl border border-slate-100 bg-white p-6 shadow-sm"><div className="flex items-center justify-between"><h3 className="font-semibold">Score overview</h3><MoreHorizontal className="size-5 text-slate-400" /></div><div className="mt-6 flex items-center justify-center"><div className="relative grid size-44 place-items-center rounded-full" style={{background:'conic-gradient(#2563eb 0 82%, #e9eef7 82% 100%)'}}><div className="grid size-32 place-items-center rounded-full bg-white"><div className="text-center"><p className="text-3xl font-bold">82%</p><p className="text-xs text-slate-400">Average score</p></div></div></div></div><div className="mt-5 grid grid-cols-3 gap-2 text-center text-xs"><div><p className="font-bold text-slate-800">95%</p><p className="mt-1 text-slate-400">Highest</p></div><div><p className="font-bold text-slate-800">72%</p><p className="mt-1 text-slate-400">Lowest</p></div><div><p className="font-bold text-slate-800">82%</p><p className="mt-1 text-slate-400">Average</p></div></div></div></div></> }

function QuizList({ quizzes, setQuizzes, student=false }: { quizzes:Quiz[]; setQuizzes:React.Dispatch<React.SetStateAction<Quiz[]>>; student?:boolean }) { const router=useRouter(); const [query,setQuery]=useState(''); const filtered=quizzes.filter(q=>q.title.toLowerCase().includes(query.toLowerCase())); return <><SectionHeader eyebrow={student?'Practice library':'Content library'} title={student?'Available quizzes':'My quizzes'} description={student?'Challenge yourself with a new assessment.':'Manage, edit, and publish your assessments.'} action={!student&&<PageButton variant="default" onClick={()=>router.push('/teacher/create-quiz')}><Plus data-icon="inline-start" />New quiz</PageButton>} /><div className="mb-5 flex items-center gap-3 rounded-xl border border-slate-100 bg-white px-3 py-2 sm:max-w-sm"><Search className="size-4 text-slate-400" /><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search quizzes..." className="w-full bg-transparent text-sm outline-none" /></div><div className="grid gap-4 lg:grid-cols-2">{filtered.map(q=><div key={q.id} className="group rounded-2xl border border-slate-100 bg-white p-5 shadow-sm transition hover:-translate-y-0.5 hover:shadow-md"><div className="flex items-start justify-between"><div className="flex items-center gap-3"><div className="grid size-11 place-items-center rounded-xl bg-blue-50 text-blue-600"><FileQuestion className="size-5" /></div><div><h3 className="font-semibold">{q.title}</h3><p className="mt-1 text-xs text-slate-400">{q.subject}</p></div></div><span className={`rounded-full px-2.5 py-1 text-[11px] font-semibold ${q.status==='Published'?'bg-emerald-50 text-emerald-600':'bg-amber-50 text-amber-600'}`}>{q.status}</span></div><p className="mt-4 text-sm leading-6 text-slate-500">{q.description}</p><div className="mt-5 grid grid-cols-3 gap-3 border-y border-slate-100 py-4 text-xs"><div><p className="font-semibold text-slate-800">{q.questions.length}</p><p className="mt-1 text-slate-400">Questions</p></div><div><p className="font-semibold text-slate-800">{q.duration} min</p><p className="mt-1 text-slate-400">Duration</p></div><div><p className="font-semibold text-slate-800">{q.totalMarks}</p><p className="mt-1 text-slate-400">Total marks</p></div></div><div className="mt-4 flex items-center justify-between"><span className="text-xs text-slate-400">{student? 'Timed assessment': `${q.attempts} attempts`}</span>{student?<PageButton variant="default" onClick={()=>router.push(`/student/quiz/${q.id}`)}>Start quiz <ChevronRight data-icon="inline-end" /></PageButton>:<div className="flex gap-2"><PageButton onClick={()=>router.push('/teacher/create-quiz')}>Edit</PageButton><PageButton onClick={()=>setQuizzes(prev=>prev.filter(x=>x.id!==q.id))}><X data-icon="inline-start" />Delete</PageButton></div>}</div></div>)}</div></> }

function CreateQuiz({ quizzes, setQuizzes }: { quizzes:Quiz[]; setQuizzes:React.Dispatch<React.SetStateAction<Quiz[]>> }) { const router=useRouter(); const [title,setTitle]=useState(''); const [subject,setSubject]=useState(''); const [description,setDescription]=useState(''); const [duration,setDuration]=useState('30'); const [questions,setQuestions]=useState<Question[]>([{id:1,text:'',options:['','','',''],correct:0,marks:10}]); const [selected,setSelected]=useState(0); const update=(patch:Partial<Question>)=>setQuestions(q=>q.map((x,i)=>i===selected?{...x,...patch}:x)); const save=(status:'Draft'|'Published')=>{const q={id:`custom-${Date.now()}`,title:title||'Untitled quiz',subject:subject||'General',description:description||'A new QuizPro assessment.',duration:Number(duration)||30,questions,status,attempts:0,totalMarks:questions.reduce((a,x)=>a+x.marks,0)};setQuizzes(prev=>[q,...prev]); router.push('/teacher/quizzes')}; return <><SectionHeader eyebrow="Quiz studio" title="Create a quiz" description="Build an assessment your students will love." action={<PageButton onClick={()=>router.push('/teacher/quizzes')}>Cancel</PageButton>} /><div className="grid gap-6 xl:grid-cols-[.85fr_1.15fr]"><div className="rounded-2xl border border-slate-100 bg-white p-6 shadow-sm"><h3 className="font-semibold">Quiz details</h3><div className="mt-6 flex flex-col gap-4"><label className="text-sm font-medium">Quiz title<input value={title} onChange={e=>setTitle(e.target.value)} placeholder="e.g. Web fundamentals" className="mt-2 w-full rounded-xl border border-slate-200 px-3 py-2.5 text-sm outline-none focus:border-blue-500" /></label><label className="text-sm font-medium">Subject<input value={subject} onChange={e=>setSubject(e.target.value)} placeholder="e.g. Computer Science" className="mt-2 w-full rounded-xl border border-slate-200 px-3 py-2.5 text-sm outline-none focus:border-blue-500" /></label><label className="text-sm font-medium">Description<textarea value={description} onChange={e=>setDescription(e.target.value)} placeholder="What will students learn?" className="mt-2 min-h-24 w-full resize-none rounded-xl border border-slate-200 px-3 py-2.5 text-sm outline-none focus:border-blue-500" /></label><div className="grid grid-cols-2 gap-3"><label className="text-sm font-medium">Duration (min)<input type="number" value={duration} onChange={e=>setDuration(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 px-3 py-2.5 text-sm outline-none" /></label><label className="text-sm font-medium">Start date<input type="date" className="mt-2 w-full rounded-xl border border-slate-200 px-3 py-2.5 text-sm outline-none" /></label></div><label className="text-sm font-medium">End date<input type="date" className="mt-2 w-full rounded-xl border border-slate-200 px-3 py-2.5 text-sm outline-none" /></label></div></div><div className="rounded-2xl border border-slate-100 bg-white p-6 shadow-sm"><div className="flex items-center justify-between"><div><h3 className="font-semibold">Question builder</h3><p className="mt-1 text-xs text-slate-400">{questions.length} questions · {questions.reduce((a,x)=>a+x.marks,0)} total marks</p></div><PageButton onClick={()=>{setQuestions(q=>[...q,{id:Date.now(),text:'',options:['','','',''],correct:0,marks:10}]);setSelected(questions.length)}}><Plus data-icon="inline-start" />Add question</PageButton></div><div className="mt-6 grid gap-5 md:grid-cols-[150px_1fr]"><div className="flex gap-2 overflow-auto md:flex-col">{questions.map((q,i)=><button key={q.id} onClick={()=>setSelected(i)} className={`flex min-w-28 items-center gap-2 rounded-xl border px-3 py-3 text-left text-xs ${i===selected?'border-blue-500 bg-blue-50 text-blue-700':'border-slate-100 text-slate-500'}`}><span className="grid size-6 place-items-center rounded-lg bg-white font-bold shadow-sm">{i+1}</span><span className="truncate">{q.text||'Untitled question'}</span></button>)}</div><div><label className="text-sm font-medium">Question text<textarea value={questions[selected]?.text||''} onChange={e=>update({text:e.target.value})} placeholder="Write your question here..." className="mt-2 min-h-24 w-full resize-none rounded-xl border border-slate-200 px-3 py-2.5 text-sm outline-none focus:border-blue-500" /></label><div className="mt-4 grid gap-3 sm:grid-cols-2">{questions[selected]?.options.map((o,i)=><label key={i} className="text-xs font-semibold text-slate-500">Option {String.fromCharCode(65+i)}<div className="mt-1 flex items-center gap-2 rounded-xl border border-slate-200 px-3"><input type="radio" name="correct" checked={questions[selected].correct===i} onChange={()=>update({correct:i})} className="accent-blue-600" /><input value={o} onChange={e=>{const options=[...questions[selected].options];options[i]=e.target.value;update({options})}} placeholder={`Answer ${String.fromCharCode(65+i)}`} className="w-full py-2.5 text-sm outline-none" /></div></label>)}</div><div className="mt-4 flex items-end gap-3"><label className="text-xs font-semibold text-slate-500">Marks<input type="number" value={questions[selected]?.marks||10} onChange={e=>update({marks:Number(e.target.value)})} className="mt-1 block w-24 rounded-xl border border-slate-200 px-3 py-2.5 text-sm outline-none" /></label><PageButton onClick={()=>{if(questions.length>1){setQuestions(q=>q.filter((_,i)=>i!==selected));setSelected(Math.max(0,selected-1))}}}><X data-icon="inline-start" />Delete</PageButton></div></div></div><div className="mt-7 flex justify-end gap-3 border-t border-slate-100 pt-5"><PageButton onClick={()=>save('Draft')}>Save draft</PageButton><PageButton variant="default" onClick={()=>save('Published')}>Publish quiz <ChevronRight data-icon="inline-end" /></PageButton></div></div></div></> }

function Leaderboard() { return <><SectionHeader eyebrow="Community" title="Leaderboard" description="See how you stack up with your peers." action={<select className="rounded-xl border border-slate-200 bg-white px-3 py-2 text-sm"><option>All quizzes</option><option>This week</option><option>This month</option></select>} /><div className="rounded-2xl border border-slate-100 bg-white p-4 shadow-sm sm:p-6"><div className="hidden grid-cols-[70px_1fr_100px_100px_100px] gap-4 border-b border-slate-100 px-4 pb-3 text-xs font-semibold uppercase tracking-wide text-slate-400 sm:grid"><span>Rank</span><span>Student</span><span>Score</span><span>Percentage</span><span>Time taken</span></div><div className="flex flex-col gap-2">{leaderboard.map(([name,score,pct,time],i)=><div key={String(name)} className={`grid items-center gap-4 rounded-xl px-4 py-4 sm:grid-cols-[70px_1fr_100px_100px_100px] ${name==='You'?'bg-blue-50':'hover:bg-slate-50'}`}><span className={`grid size-8 place-items-center rounded-lg text-sm font-bold ${i<3?'bg-amber-100 text-amber-700':'bg-slate-100 text-slate-500'}`}>{i+1}</span><div className="flex items-center gap-3"><div className="grid size-9 place-items-center rounded-full bg-indigo-100 text-xs font-bold text-indigo-700">{String(name).split(' ').map(x=>x[0]).join('')}</div><span className="text-sm font-semibold">{name}</span>{name==='You'&&<span className="rounded-full bg-blue-600 px-2 py-0.5 text-[10px] font-semibold text-white">You</span>}</div><span className="text-sm font-bold">{score}</span><span className="text-sm text-slate-500">{pct}</span><span className="text-sm text-slate-500">{time}</span></div>)}</div></div></> }

function Results({ attempts }: { attempts:Attempt[] }) { return <><SectionHeader eyebrow="Insights" title="Responses & results" description="Track how your students are performing." /><div className="grid gap-4 sm:grid-cols-3"><StatCard label="Average score" value="82%" detail="+6.4% from last month" icon={BarChart3} tone="bg-blue-50 text-blue-600"/><StatCard label="Highest score" value="98%" detail="Computer Networks" icon={Trophy} tone="bg-amber-50 text-amber-600"/><StatCard label="Total attempts" value="359" detail="Across all published quizzes" icon={Users} tone="bg-violet-50 text-violet-600"/></div><div className="mt-6 overflow-hidden rounded-2xl border border-slate-100 bg-white shadow-sm"><div className="flex items-center justify-between p-6"><div><h3 className="font-semibold">Recent attempts</h3><p className="mt-1 text-sm text-slate-400">Latest student submissions</p></div><PageButton>Export CSV</PageButton></div><div className="overflow-x-auto"><table className="w-full min-w-[700px] text-left text-sm"><thead className="border-y border-slate-100 bg-slate-50 text-xs uppercase tracking-wide text-slate-400"><tr><th className="px-6 py-3">Student name</th><th>Quiz</th><th>Score</th><th>Time taken</th><th>Tab switches</th><th>Status</th></tr></thead><tbody>{[...attempts,...leaderboard.slice(0,3).map((x,i)=>({id:String(i),quizName:'Computer Networks',score:Number(x[1]),percentage:Number(x[1]),date:'Today',status:'Completed',time:String(x[3]),tabSwitches:i%2}))].slice(0,6).map((a,i)=><tr key={a.id} className="border-b border-slate-50"><td className="px-6 py-4 font-medium">{['Alex Morgan','Jordan Lee','Priya Shah','Sam Wilson','Taylor Kim','Morgan Lee'][i]}</td><td className="text-slate-500">{a.quizName}</td><td className="font-semibold">{a.score}/100</td><td className="text-slate-500">{a.time}</td><td className="text-slate-500">{a.tabSwitches}</td><td><span className="rounded-full bg-emerald-50 px-2.5 py-1 text-xs font-semibold text-emerald-600">{a.status}</span></td></tr>)}</tbody></table></div></div></> }

function Instructions({ quiz, onStart }: { quiz:Quiz; onStart:()=>void }) { return <div className="mx-auto max-w-3xl"><div className="mb-8 text-center"><div className="mx-auto mb-5 grid size-16 place-items-center rounded-2xl bg-blue-600 text-white shadow-lg shadow-blue-200"><FileQuestion className="size-8" /></div><p className="text-sm font-semibold text-blue-600">Before you begin</p><h2 className="mt-2 text-3xl font-bold tracking-tight">Quiz instructions</h2><p className="mt-2 text-slate-500">{quiz.title} · {quiz.subject}</p></div><div className="rounded-2xl border border-slate-100 bg-white p-6 shadow-sm sm:p-8"><div className="grid grid-cols-3 divide-x rounded-2xl bg-slate-50 py-4 text-center"><div><p className="text-2xl font-bold">{quiz.duration}</p><p className="mt-1 text-xs text-slate-400">Minutes</p></div><div><p className="text-2xl font-bold">{quiz.questions.length}</p><p className="mt-1 text-xs text-slate-400">Questions</p></div><div><p className="text-2xl font-bold">{quiz.totalMarks}</p><p className="mt-1 text-xs text-slate-400">Total marks</p></div></div><div className="mt-8"><h3 className="font-semibold">Please keep in mind</h3><ul className="mt-4 flex flex-col gap-3 text-sm leading-6 text-slate-500">{['Read each question carefully before selecting an answer.','The quiz has a fixed time limit and will submit automatically.','Do not switch browser tabs during the quiz.','Switching tabs will be detected and logged.','Multiple violations may automatically terminate the quiz.','Once submitted, the quiz cannot be restarted.'].map(x=><li key={x} className="flex gap-3"><Check className="mt-1 size-4 shrink-0 text-emerald-500" />{x}</li>)}</ul></div><div className="mt-8 flex justify-center"><PageButton variant="default" onClick={onStart}>Start quiz <ChevronRight data-icon="inline-end" /></PageButton></div></div><p className="mt-5 text-center text-xs text-slate-400">This is a frontend demo. Browser tab detection is not a substitute for robust proctoring.</p></div> }

function QuizAttempt({ quiz, onFinish }: { quiz:Quiz; onFinish:(score:number, switches:number, time:string)=>void }) { const [index,setIndex]=useState(0); const [answers,setAnswers]=useState<Record<number,number>>({}); const [seconds,setSeconds]=useState(quiz.duration*60); const [switches,setSwitches]=useState(0); const [warning,setWarning]=useState(false); const [confirm,setConfirm]=useState(false); const finish=(forced=false)=>{const correct=quiz.questions.reduce((a,q,i)=>a+(answers[q.id]===q.correct?q.marks:0),0);onFinish(correct,switches,`${Math.floor((quiz.duration*60-seconds)/60).toString().padStart(2,'0')}:${((quiz.duration*60-seconds)%60).toString().padStart(2,'0')}`)}; useEffect(()=>{const timer=setInterval(()=>setSeconds(s=>{if(s<=1){clearInterval(timer);finish(true);return 0}return s-1}),1000);const visibility=()=>{if(document.hidden){setSwitches(s=>{const next=s+1;if(next>=3) finish(true); else setWarning(true);return next})}};const blur=()=>{if(document.visibilityState==='visible')setWarning(true)};document.addEventListener('visibilitychange',visibility);window.addEventListener('blur',visibility);return()=>{clearInterval(timer);document.removeEventListener('visibilitychange',visibility);window.removeEventListener('blur',visibility)}},[]);const q=quiz.questions[index]; return <div className="mx-auto max-w-6xl"><div className="mb-5 flex flex-wrap items-center justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-widest text-blue-600">Live assessment</p><h2 className="mt-1 text-xl font-bold">{quiz.title}</h2><p className="mt-1 text-sm text-slate-400">Question {index+1} of {quiz.questions.length}</p></div><div className={`flex items-center gap-2 rounded-xl px-4 py-3 text-sm font-bold ${seconds<=60?'bg-red-50 text-red-600':'bg-blue-50 text-blue-700'}`}><Clock3 className="size-4" /> Time remaining: {Math.floor(seconds/60).toString().padStart(2,'0')}:{(seconds%60).toString().padStart(2,'0')}</div></div><div className="grid gap-5 lg:grid-cols-[1fr_260px]"><div className="rounded-2xl border border-slate-100 bg-white p-6 shadow-sm sm:p-8"><div className="mb-8 flex items-center justify-between"><span className="rounded-full bg-blue-50 px-3 py-1 text-xs font-semibold text-blue-700">Question {index+1}</span><span className="text-sm text-slate-400">{q.marks} marks</span></div><h3 className="text-xl font-semibold leading-8">{q.text}</h3><div className="mt-8 flex flex-col gap-3">{q.options.map((o,i)=><label key={i} className={`flex cursor-pointer items-center gap-4 rounded-xl border p-4 transition ${answers[q.id]===i?'border-blue-500 bg-blue-50':'border-slate-100 hover:border-blue-200'}`}><input type="radio" name={`q-${q.id}`} checked={answers[q.id]===i} onChange={()=>setAnswers({...answers,[q.id]:i})} className="size-4 accent-blue-600" /><span className="grid size-7 place-items-center rounded-lg bg-slate-100 text-xs font-bold text-slate-500">{String.fromCharCode(65+i)}</span><span className="text-sm font-medium">{o||`Option ${String.fromCharCode(65+i)}`}</span></label>)}</div><div className="mt-9 flex items-center justify-between border-t border-slate-100 pt-5"><PageButton onClick={()=>setIndex(Math.max(0,index-1))}><ChevronLeft data-icon="inline-start" />Previous</PageButton>{index===quiz.questions.length-1?<PageButton variant="default" onClick={()=>setConfirm(true)}>Submit quiz <Check data-icon="inline-end" /></PageButton>:<PageButton variant="default" onClick={()=>setIndex(index+1)}>Next question <ChevronRight data-icon="inline-end" /></PageButton>}</div></div><div className="rounded-2xl border border-slate-100 bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><h3 className="font-semibold">Question palette</h3><span className="text-xs text-slate-400">{Object.keys(answers).length}/{quiz.questions.length}</span></div><div className="mt-5 grid grid-cols-5 gap-2">{quiz.questions.map((item,i)=><button key={item.id} onClick={()=>setIndex(i)} className={`grid size-9 place-items-center rounded-lg text-xs font-semibold ${i===index?'bg-blue-600 text-white':answers[item.id]!==undefined?'bg-emerald-50 text-emerald-600':'bg-slate-50 text-slate-500'}`}>{i+1}</button>)}</div><div className="mt-6 flex flex-col gap-2 text-xs text-slate-400"><div className="flex items-center gap-2"><span className="size-2 rounded-full bg-blue-600"/>Current</div><div className="flex items-center gap-2"><span className="size-2 rounded-full bg-emerald-500"/>Answered</div><div className="flex items-center gap-2"><span className="size-2 rounded-full bg-slate-200"/>Unanswered</div></div><div className="mt-7 rounded-xl bg-orange-50 p-3 text-xs leading-5 text-orange-700"><ShieldAlert className="mb-2 size-4" />Tab switches: <strong>{switches} / 3</strong></div></div></div>{warning&&<div className="fixed inset-0 z-40 grid place-items-center bg-slate-950/35 p-5"><div className="w-full max-w-sm rounded-2xl bg-white p-6 text-center shadow-2xl"><div className="mx-auto grid size-12 place-items-center rounded-full bg-orange-50 text-orange-600"><ShieldAlert className="size-6" /></div><h3 className="mt-4 text-lg font-bold">Warning: Tab switching detected</h3><p className="mt-2 text-sm leading-6 text-slate-500">Your quiz attempt has been flagged. Tab switches: {switches} / 3.</p><PageButton variant="default" onClick={()=>setWarning(false)}>Return to quiz</PageButton></div></div>}{confirm&&<div className="fixed inset-0 z-40 grid place-items-center bg-slate-950/35 p-5"><div className="w-full max-w-sm rounded-2xl bg-white p-6 shadow-2xl"><h3 className="text-lg font-bold">Are you sure you want to submit?</h3><p className="mt-2 text-sm leading-6 text-slate-500">You answered {Object.keys(answers).length} of {quiz.questions.length} questions. This quiz cannot be restarted.</p><div className="mt-6 flex justify-end gap-3"><PageButton onClick={()=>setConfirm(false)}>Cancel</PageButton><PageButton variant="default" onClick={()=>finish()}>Submit quiz</PageButton></div></div></div>}</div> }

function Result({ quiz, score, switches, time, attempts }: { quiz:Quiz; score:number; switches:number; time:string; attempts:Attempt[] }) { const router=useRouter(); const percentage=Math.round(score/quiz.totalMarks*100); return <div className="mx-auto max-w-4xl"><div className="text-center"><div className="mx-auto grid size-16 place-items-center rounded-2xl bg-emerald-500 text-white shadow-lg shadow-emerald-200"><Trophy className="size-8" /></div><p className="mt-5 text-sm font-semibold text-emerald-600">Quiz Completed Successfully</p><h2 className="mt-2 text-3xl font-bold tracking-tight">Great work, Alex.</h2><p className="mt-2 text-slate-500">Here’s how you performed on {quiz.title}.</p></div><div className="mt-8 rounded-3xl bg-gradient-to-br from-blue-600 to-indigo-700 p-8 text-center text-white shadow-xl shadow-blue-200"><p className="text-sm text-blue-100">Your score</p><p className="mt-2 text-6xl font-bold">{score} <span className="text-2xl font-medium text-blue-200">/ {quiz.totalMarks}</span></p><div className="mx-auto mt-5 h-2 max-w-sm overflow-hidden rounded-full bg-white/20"><div className="h-full rounded-full bg-white" style={{width:`${percentage}%`}} /></div><p className="mt-3 text-lg font-semibold">{percentage}%</p></div><div className="mt-5 grid grid-cols-2 gap-3 sm:grid-cols-5">{[['Correct answers',String(Math.round(score/10)),Check],['Wrong answers',String(quiz.questions.length-Math.round(score/10)-1),X],['Unanswered',String(Math.max(0,quiz.questions.length-Object.keys({}).length)),CircleHelp],['Time taken',time,Clock3],['Tab switches',String(switches),ShieldAlert]].map(([label,value,Icon])=><div key={String(label)} className="rounded-2xl border border-slate-100 bg-white p-4 text-center shadow-sm"><Icon className="mx-auto size-4 text-blue-600" /><p className="mt-2 text-lg font-bold">{String(value)}</p><p className="mt-1 text-[11px] text-slate-400">{String(label)}</p></div>)}</div><div className="mt-8 flex flex-wrap justify-center gap-3"><PageButton onClick={()=>alert('Answer review is available in this demo.')}>View answers</PageButton><PageButton variant="default" onClick={()=>router.push('/student')}>Back to dashboard</PageButton><PageButton onClick={()=>router.push('/student/leaderboard')}>View leaderboard</PageButton></div></div> }

function Attempts({ attempts }: { attempts:Attempt[] }) { const router=useRouter(); return <><SectionHeader eyebrow="Your progress" title="My attempts" description="Review your recent quiz performance." /><div className="grid gap-3">{attempts.length===0&&<div className="rounded-2xl border border-dashed border-slate-200 bg-white p-10 text-center text-sm text-slate-400">No attempts yet. Start a quiz to see your results here.</div>}{attempts.map(a=><div key={a.id} className="flex flex-col gap-4 rounded-2xl border border-slate-100 bg-white p-5 shadow-sm sm:flex-row sm:items-center"><div className="grid size-11 place-items-center rounded-xl bg-blue-50 text-blue-600"><FileQuestion className="size-5" /></div><div className="min-w-0 flex-1"><h3 className="font-semibold">{a.quizName}</h3><p className="mt-1 text-xs text-slate-400">{a.date} · {a.time}</p></div><div className="flex items-center gap-5"><div><p className="font-bold">{a.score}</p><p className="text-xs text-slate-400">Score</p></div><div><p className="font-bold text-blue-600">{a.percentage}%</p><p className="text-xs text-slate-400">Percentage</p></div><span className="rounded-full bg-emerald-50 px-2.5 py-1 text-xs font-semibold text-emerald-600">{a.status}</span><PageButton onClick={()=>router.push(`/student/result/${a.quizId}`)}>View result</PageButton></div></div>)}</div></> }

export default function Page() { const pathname=usePathname(); const router=useRouter(); const [quizzes,setQuizzes]=useLocalState<Quiz[]>('quizpro-quizzes',initialQuizzes); const [attempts,setAttempts]=useLocalState<Attempt[]>('quizpro-attempts',[]); const [result,setResult]=useState<{quiz:Quiz;score:number;switches:number;time:string}|null>(null); const [readyToStart,setReadyToStart]=useState(false); if(pathname==='/'||!pathname)return <Landing/>; const teacher=pathname.startsWith('/teacher'); const student=pathname.startsWith('/student'); const quizId=pathname.split('/')[3]||pathname.split('/')[2]; const quiz=quizzes.find(q=>q.id===quizId)||quizzes[0]; if(pathname.startsWith('/student/quiz/')&&quiz){ if(result&&result.quiz.id===quiz.id)return <AppShell role="student"><Result {...result} quiz={quiz} attempts={attempts}/></AppShell>; return <AppShell role="student">{readyToStart?<QuizAttempt quiz={quiz} onFinish={(score,switches,time)=>{const next={id:Date.now().toString(),quizId:quiz.id,quizName:quiz.title,date:'Today',score,percentage:Math.round(score/quiz.totalMarks*100),status:'Completed',time,tabSwitches:switches};setAttempts(a=>[next,...a]);setResult({quiz,score,switches,time});router.push(`/student/result/${quiz.id}`)}} />:<Instructions quiz={quiz} onStart={()=>setReadyToStart(true)} />}</AppShell> } if(pathname.startsWith('/student/result/')&&quiz)return <AppShell role="student"><Result quiz={quiz} score={result?.score||32} switches={result?.switches||0} time={result?.time||'15:14'} attempts={attempts}/></AppShell>; return <AppShell role={teacher?'teacher':'student'}>{teacher&&pathname==='/teacher'&&<Dashboard role="teacher" quizzes={quizzes} attempts={attempts}/>} {teacher&&pathname==='/teacher/create-quiz'&&<CreateQuiz quizzes={quizzes} setQuizzes={setQuizzes}/>} {teacher&&pathname==='/teacher/quizzes'&&<QuizList quizzes={quizzes} setQuizzes={setQuizzes}/>} {teacher&&pathname==='/teacher/results'&&<Results attempts={attempts}/>} {teacher&&pathname==='/teacher/leaderboard'&&<Leaderboard/>} {student&&pathname==='/student'&&<Dashboard role="student" quizzes={quizzes} attempts={attempts}/>} {student&&pathname==='/student/quizzes'&&<QuizList quizzes={quizzes} setQuizzes={setQuizzes} student/>} {student&&pathname==='/student/attempts'&&<Attempts attempts={attempts}/>} {student&&pathname==='/student/leaderboard'&&<Leaderboard/>}</AppShell> }

export { }
